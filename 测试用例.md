# 外卖拼单系统 - 测试用例

## 目录

1. [用户模块测试用例](#1-用户模块测试用例)
2. [商家模块测试用例](#2-商家模块测试用例)
3. [拼单模块测试用例](#3-拼单模块测试用例)
4. [订单模块测试用例](#4-订单模块测试用例)
5. [支付模块测试用例](#5-支付模块测试用例)
6. [网关与鉴权测试用例](#6-网关与鉴权测试用例)
7. [前端功能测试用例](#7-前端功能测试用例)
8. [集成测试用例（端到端）](#8-集成测试用例端到端)

---

## 1. 用户模块测试用例

### UC-101 用户注册 - 正常注册

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-101 |
| 测试目标 | 验证新用户可以使用未注册手机号成功注册 |
| 前置条件 | 手机号 13800001111 未注册 |
| 请求 | `POST /api/v1/users/register` |
| 请求体 | `{"phone":"13800001111","code":"1234","nickname":"测试用户A"}` |
| 预期响应 | `{"code":0,"data":{"userId":"USR...","phone":"138****1111","nickname":"测试用户A","token":"eyJ..."}}` |
| 验证点 | 1. 返回 code=0 2. userId 非空 3. 手机号脱敏显示 4. token 非空 5. 昵称正确 |

### UC-102 用户注册 - 手机号已注册

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-102 |
| 测试目标 | 验证重复手机号注册返回错误 |
| 前置条件 | 手机号 13800001111 已注册 |
| 请求 | `POST /api/v1/users/register` |
| 请求体 | `{"phone":"13800001111","code":"1234","nickname":"重复用户"}` |
| 预期响应 | `{"code":400,"message":"手机号已注册"}` |
| 验证点 | 1. 返回 code=400 2. 包含"手机号已注册"提示 |

### UC-103 用户注册 - 缺少必填字段

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-103 |
| 测试目标 | 验证缺少 phone/nickname 时返回错误 |
| 请求 | `POST /api/v1/users/register` |
| 请求体 | `{"phone":"13800002222"}`（缺少 nickname） |
| 预期响应 | 服务端应返回错误码或前端校验拦截 |
| 验证点 | 注册不成功 |

### UC-104 用户登录 - 正常登录

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-104 |
| 测试目标 | 验证已注册用户可正常登录 |
| 前置条件 | 用户 13800001111 已注册 |
| 请求 | `POST /api/v1/users/login` |
| 请求体 | `{"phone":"13800001111","code":"1234"}` |
| 预期响应 | `{"code":0,"data":{"userId":"USR...","token":"eyJ..."}}` |
| 验证点 | 1. code=0 2. token 非空 3. userId 与注册时一致 |

### UC-105 用户登录 - 用户不存在

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-105 |
| 测试目标 | 验证未注册手机号登录返回错误 |
| 请求 | `POST /api/v1/users/login` |
| 请求体 | `{"phone":"13999999999","code":"1234"}` |
| 预期响应 | `{"code":404,"message":"用户不存在"}` |
| 验证点 | 1. code=404 2. 提示"用户不存在" |

### UC-106 获取用户信息

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-106 |
| 测试目标 | 验证通过 userId 获取用户信息 |
| 前置条件 | 已登录，获取 userId 和 token |
| 请求 | `GET /api/v1/users/{userId}` |
| Headers | `Authorization: Bearer {token}` |
| 预期响应 | `{"code":0,"data":{"userId":"USR...","nickname":"测试用户A"}}` |
| 验证点 | 1. code=0 2. 返回正确的用户信息 |

### UC-107 新增收货地址

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-107 |
| 测试目标 | 验证用户可以新增收货地址 |
| 前置条件 | 已登录 |
| 请求 | `POST /api/v1/users/addresses` |
| Headers | `Authorization: Bearer {token}` |
| 请求体 | `{"label":"公司","detail":"科技园A座10楼","phone":"13800001111","isDefault":true}` |
| 预期响应 | `{"code":0,"data":{"addressId":"ADR...","label":"公司"}}` |
| 验证点 | 1. code=0 2. addressId 非空 3. 信息完整 |

### UC-108 查询收货地址列表

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-108 |
| 测试目标 | 验证查询用户收货地址列表 |
| 前置条件 | 已有 ≥1 个地址 |
| 请求 | `GET /api/v1/users/{userId}/addresses` |
| Headers | `Authorization: Bearer {token}` |
| 预期响应 | `{"code":0,"data":[{"addressId":"ADR...","label":"公司",...}]}` |
| 验证点 | 1. code=0 2. 返回列表非空 |

---

## 2. 商家模块测试用例

### UC-201 查询商家列表 - 全部

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-201 |
| 测试目标 | 验证不带分类参数时返回所有商家 |
| 前置条件 | 商家表有数据 |
| 请求 | `GET /api/v1/merchants` |
| 预期响应 | `{"code":0,"data":[{"merchantId":"M001","name":"...",...}]}` |
| 验证点 | 1. code=0 2. 返回 ≥1 条数据 3. 包含 imageUrl 字段 |

### UC-202 查询商家列表 - 按分类筛选

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-202 |
| 测试目标 | 验证按分类筛选商家 |
| 请求 | `GET /api/v1/merchants?category=川湘菜` |
| 预期响应 | 返回的商家 category 均为"川湘菜" |
| 验证点 | 1. code=0 2. 所有结果分类一致 |

### UC-203 查询商家列表 - 不存在的分类

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-203 |
| 测试目标 | 验证查询不存在的分类返回空列表 |
| 请求 | `GET /api/v1/merchants?category=不存在的分类` |
| 预期响应 | `{"code":0,"data":[]}` |
| 验证点 | 1. code=0 2. data 为空数组 |

### UC-204 查询商家详情

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-204 |
| 测试目标 | 验证获取商家详情 |
| 请求 | `GET /api/v1/merchants/M001` |
| 预期响应 | 包含 name, category, rating, imageUrl, deliveryFee 等字段 |
| 验证点 | 1. code=0 2. 字段完整 3. 无 JWT 也可访问 |

### UC-205 查询商家详情 - 不存在

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-205 |
| 测试目标 | 验证查询不存在的商家返回错误 |
| 请求 | `GET /api/v1/merchants/M999` |
| 预期响应 | 错误或空数据 |
| 验证点 | 不会导致服务异常 |

### UC-206 查询菜品列表

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-206 |
| 测试目标 | 验证获取某商家的菜品列表 |
| 请求 | `GET /api/v1/merchants/M001/items` |
| 预期响应 | 包含 itemId, name, price, imageUrl, category 等 |
| 验证点 | 1. code=0 2. 菜品非空 3. price 为正整数（分） 4. 有 imageUrl |

### UC-207 菜品价格验证

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-207 |
| 测试目标 | 验证所有菜品价格 > 0 |
| 请求 | `GET /api/v1/merchants/M001/items` |
| 验证点 | 每个 item.price > 0 |

---

## 3. 拼单模块测试用例

### UC-301 创建拼单 - 正常

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-301 |
| 测试目标 | 验证用户可以创建拼单 |
| 前置条件 | 已登录，有地址 |
| 请求 | `POST /api/v1/pools` |
| Headers | `Authorization: Bearer {token}, X-User-Id: {userId}` |
| 请求体 | `{"merchantId":"M001","merchantName":"川味人家","creatorAddressId":"ADR001","formationRule":{"minMembers":2,"minAmount":0,"deadlineMinutes":30},"remark":"微辣"}` |
| 预期响应 | `{"code":0,"data":{"poolId":"POL...","status":"FORMING","currentMembers":1,"inviteCode":"ABCDEF"}}` |
| 验证点 | 1. poolId 非空 2. 状态为 FORMING 3. 人数为 1 4. 邀请码 6 位 5. 创建者自动参与 |

### UC-302 创建拼单 - 无地址

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-302 |
| 测试目标 | 验证创建拼单时无地址的情况 |
| 请求体 | `{"merchantId":"M001","merchantName":"川味人家","creatorAddressId":"","formationRule":{"minMembers":2}}` |
| 预期响应 | 前端拦截或后端报错 |
| 验证点 | 拼单不应创建成功 |

### UC-303 创建拼单 - 默认成团规则

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-303 |
| 测试目标 | 验证不传 formationRule 时使用默认值 |
| 请求体 | `{"merchantId":"M001","merchantName":"川味人家","creatorAddressId":"ADR001"}` |
| 预期响应 | minMembers=2, minAmount=0, deadlineMinutes=30 |
| 验证点 | 使用默认规则创建成功 |

### UC-304 加入拼单 - 正常

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-304 |
| 测试目标 | 验证第二人加入拼单 |
| 前置条件 | 拼单 POL001 状态为 FORMING |
| 请求 | `POST /api/v1/pools/POL001/join` |
| Headers | `Authorization: Bearer {token2}` |
| 请求体 | `{"addressId":"ADR002","items":[{"itemId":"F001","itemName":"麻辣香锅","unitPrice":3500,"quantity":1}],"remark":"不要葱"}` |
| 预期响应 | `{"code":0,"data":{"poolId":"POL001","currentMembers":2,...}}` |
| 验证点 | 1. code=0 2. 人数增加为 2 3. 金额更新 |

### UC-305 加入拼单 - 达到成团条件自动成团

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-305 |
| 测试目标 | 验证加入后达到 minMembers 自动成团 |
| 前置条件 | 拼单 minMembers=2，当前 1 人 |
| 请求 | 第二人加入 |
| 预期响应 | status 变为 FORMED 或触发成团事件 |
| 验证点 | 状态流转正确 |

### UC-306 加入拼单 - 重复加入

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-306 |
| 测试目标 | 验证同一用户不能重复加入 |
| 前置条件 | 用户 A 已加入拼单 POL001 |
| 请求 | 用户 A 再次 POST /api/v1/pools/POL001/join |
| 预期响应 | `{"code":409,"message":"已加入该拼单"}` |
| 验证点 | 返回冲突错误 |

### UC-307 加入拼单 - 拼单已结束

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-307 |
| 测试目标 | 验证不能加入已解散/已成团的拼单 |
| 前置条件 | 拼单状态为 DISSOLVED |
| 预期响应 | `{"code":409,"message":"拼单已不在进行中"}` |
| 验证点 | 拒绝加入 |

### UC-308 退出拼单 - 普通成员退出

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-308 |
| 测试目标 | 验证普通成员可以退出拼单 |
| 前置条件 | 拼单有 ≥3 人，用户 B 为 MEMBER |
| 请求 | `POST /api/v1/pools/POL001/leave` |
| Headers | `Authorization: Bearer {tokenB}` |
| 预期响应 | `{"code":0}` |
| 验证点 | 1. code=0 2. 人数减少 3. 金额减少 |

### UC-309 退出拼单 - 发起者退出导致解散

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-309 |
| 测试目标 | 验证发起者退出后拼单自动解散 |
| 前置条件 | 发起者为 CREATOR |
| 请求 | CREATOR 调用 leave |
| 预期响应 | code=0，拼单状态变为 DISSOLVED |
| 验证点 | 拼单被解散 |

### UC-310 退出拼单 - 未参与

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-310 |
| 测试目标 | 验证未参与用户不能退出 |
| 请求 | 未参与用户调用 leave |
| 预期响应 | `{"code":404,"message":"未参与该拼单"}` |
| 验证点 | 返回未参与错误 |

### UC-311 查询拼单详情

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-311 |
| 测试目标 | 验证获取拼单详情 |
| 请求 | `GET /api/v1/pools/POL001` |
| Headers | `Authorization: Bearer {token}` |
| 预期响应 | 包含 poolId, merchantName, status, participants, formationRule 等 |
| 验证点 | 1. code=0 2. 参与者列表完整 3. 每人菜品明细正确 |

### UC-312 查询拼单详情 - 不存在

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-312 |
| 测试目标 | 验证查询不存在的拼单 |
| 请求 | `GET /api/v1/pools/POL999` |
| 预期响应 | `{"code":404,"message":"拼单不存在"}` |
| 验证点 | 返回 404 |

### UC-313 我的拼单列表

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-313 |
| 测试目标 | 验证查询用户参与的拼单列表 |
| 请求 | `GET /api/v1/pools/my` |
| 验证点 | 1. code=0 2. 返回用户参与的拼单 3. 分页正常 |

### UC-314 我的拼单 - 按状态筛选

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-314 |
| 测试目标 | 验证按状态筛选拼单 |
| 请求 | `GET /api/v1/pools/my?status=FORMING` |
| 验证点 | 所有返回项 status=FORMING |

### UC-315 可加入拼单列表

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-315 |
| 测试目标 | 验证查询可加入的拼单列表 |
| 请求 | `GET /api/v1/pools/available?page=1&size=10` |
| 验证点 | 1. code=0 2. 不含用户已参与的 3. 状态为 FORMING/CREATED 4. 未过期 |

### UC-316 添加菜品到拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-316 |
| 测试目标 | 验证已参与用户可以添加菜品 |
| 前置条件 | 用户 A 已加入拼单 POL001 |
| 请求 | `POST /api/v1/pools/POL001/items` |
| 请求体 | `[{"itemId":"F002","itemName":"宫保鸡丁","unitPrice":2800,"quantity":2}]` |
| 预期响应 | code=0，菜品金额增加 |
| 验证点 | 1. 菜品添加成功 2. 金额正确累加 3. CAS 版本更新 |

### UC-317 添加菜品 - 未参与拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-317 |
| 测试目标 | 验证未参与用户不能添加菜品 |
| 请求 | 未参与用户调用 addItems |
| 预期响应 | `{"code":404,"message":"未参与该拼单"}` |
| 验证点 | 拒绝添加 |

### UC-318 并发加入拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-318 |
| 测试目标 | 验证多人同时加入拼单的并发安全性 |
| 操作 | 5 个用户几乎同时调用 join |
| 验证点 | 1. 无异常 2. 最终人数正确 3. 金额汇总正确 4. CAS 或锁保护有效 |

---

## 4. 订单模块测试用例

### UC-401 查询订单详情

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-401 |
| 测试目标 | 验证获取订单详情 |
| 前置条件 | 拼单成团后订单已创建 |
| 请求 | `GET /api/v1/orders/ORD001` |
| Headers | `Authorization: Bearer {token}` |
| 预期响应 | 包含 orderId, poolId, merchantName, status, totalAmount, items, participants |
| 验证点 | 1. code=0 2. 费用分摊数据完整 |

### UC-402 查询我的订单列表

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-402 |
| 测试目标 | 验证查询用户订单列表 |
| 请求 | `GET /api/v1/orders?status=&page=1&size=10` |
| 验证点 | 1. code=0 2. myPayAmount 正确 |

### UC-403 取消订单 - 正常

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-403 |
| 测试目标 | 验证 PENDING_CONFIRM 状态的订单可取消 |
| 前置条件 | 订单状态为 PENDING_CONFIRM |
| 请求 | `POST /api/v1/orders/ORD001/cancel` |
| 预期响应 | `{"code":0}` |
| 验证点 | 1. 订单状态变更为 CANCELLED 2. 触发取消事件 |

### UC-404 取消订单 - 已确认不可取消

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-404 |
| 测试目标 | 验证 CONFIRMED 状态的订单不可取消 |
| 前置条件 | 订单状态为 CONFIRMED |
| 预期响应 | `{"code":409,"message":"订单已确认，无法取消"}` |
| 验证点 | 拒绝取消 |

### UC-405 取消订单 - 订单不存在

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-405 |
| 测试目标 | 验证取消不存在的订单 |
| 请求 | `POST /api/v1/orders/ORD999/cancel` |
| 预期响应 | `{"code":404,"message":"订单不存在"}` |
| 验证点 | 返回 404 |

---

## 5. 支付模块测试用例

### UC-501 拼单扣款

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-501 |
| 测试目标 | 验证对拼单所有成员批量扣款 |
| 请求 | `POST /api/v1/payments/pool/POL001/charge` |
| 请求体 | `{"orderId":"ORD001","charges":[{"userId":"USR001","foodAmount":3500,"deliveryShare":200,"packagingShare":100,"totalAmount":3800},...]}` |
| 预期响应 | `{"code":0,"data":{"paymentBatchId":"PB...","totalCharged":...}}` |
| 验证点 | 1. code=0 2. 每人扣款金额正确 3. 总额正确 |

### UC-502 拼单退款

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-502 |
| 测试目标 | 验证拼单取消后退款 |
| 前置条件 | 拼单 POL001 已扣款 |
| 请求 | `POST /api/v1/payments/pool/POL001/refund` |
| 请求体 | `{"reason":"拼单解散"}` |
| 预期响应 | `{"code":0,"data":{"totalRefunded":...}}` |
| 验证点 | 1. 退款金额 = 扣款金额 2. 每人退款记录生成 |

### UC-503 查询结算信息

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-503 |
| 测试目标 | 验证查询拼单的费用结算 |
| 请求 | `GET /api/v1/payments/pool/POL001/settlement` |
| 预期响应 | 包含 splitMethod, commonFees, settlements |
| 验证点 | 1. code=0 2. 费用分摊计算正确 3. 配送费按人头均摊，余数给最后一个人 |

### UC-504 查询用户支付记录

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-504 |
| 测试目标 | 验证查询用户支付记录列表 |
| 请求 | `GET /api/v1/payments/user/USR001/records?page=1&size=10` |
| 验证点 | 1. code=0 2. 包含 CHARGE 和 REFUND 类型 3. 分页正确 |

### UC-505 费用分摊算法验证

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-505 |
| 测试目标 | 验证配送费/包装费均摊算法，余数分配给最后一个人 |
| 场景 | 配送费 100 分，3 人 → 每人 33 分，最后一人 34 分 |
| 验证点 | 总和 = 配送费 + 包装费 + 菜品费，无遗漏 |

---

## 6. 网关与鉴权测试用例

### UC-601 JWT 验证 - 有效 Token

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-601 |
| 测试目标 | 验证有效 JWT 可以通过网关 |
| 请求 | `GET /api/v1/pools/my` + `Authorization: Bearer {valid_token}` |
| 验证点 | 1. 请求通过网关 2. 下游服务收到 X-User-Id header |

### UC-602 JWT 验证 - 无 Token

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-602 |
| 测试目标 | 验证无 Token 请求被网关拒绝 |
| 请求 | `GET /api/v1/pools/my`（无 Authorization header） |
| 预期响应 | HTTP 401 |
| 验证点 | 返回 401 未授权 |

### UC-603 JWT 验证 - 无效 Token

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-603 |
| 测试目标 | 验证伪造/过期 Token 被拒绝 |
| 请求 | `GET /api/v1/pools/my` + `Authorization: Bearer invalid.token.here` |
| 预期响应 | HTTP 401 |
| 验证点 | 返回 401 |

### UC-604 白名单路径 - 注册无需 Token

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-604 |
| 测试目标 | 验证注册接口无需 JWT |
| 请求 | `POST /api/v1/users/register`（无 Authorization） |
| 验证点 | 请求正常到达 user-service |

### UC-605 白名单路径 - 商家列表无需 Token

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-605 |
| 测试目标 | 验证商家接口无需 JWT |
| 请求 | `GET /api/v1/merchants`（无 Authorization） |
| 验证点 | 返回商家列表 |

### UC-606 CORS 跨域测试

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-606 |
| 测试目标 | 验证前端（localhost:3000）可跨域访问网关（localhost:8080） |
| 操作 | 浏览器中访问前端页面，检查网络请求 |
| 验证点 | 无 CORS 报错，OPTIONS 预检返回正确 headers |

---

## 7. 前端功能测试用例

### UC-701 登录页面显示

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-701 |
| 测试目标 | 验证登录页面正确显示 |
| 操作 | 浏览器打开 http://localhost:3000/index.html |
| 验证点 | 1. 显示 logo 和"拼单外卖"标题 2. 有登录/注册切换按钮 3. 有手机号和验证码输入框 |

### UC-702 用户注册流程

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-702 |
| 测试目标 | 验证前端注册流程 |
| 操作 | 点击"注册" → 填写手机号/验证码/昵称 → 点击"注册" |
| 验证点 | 1. 注册成功后自动跳转主页 2. 显示用户昵称 |

### UC-703 用户登录流程

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-703 |
| 测试目标 | 验证前端登录流程 |
| 操作 | 填写已注册手机号 → 点击"登录" |
| 验证点 | 1. 登录成功跳转主页 2. localStorage 保存用户信息 |

### UC-704 首页 - 分类导航栏

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-704 |
| 测试目标 | 验证首页分类药丸显示和切换 |
| 操作 | 进入首页，查看分类栏 |
| 验证点 | 1. 显示 9 个分类（含"全部"） 2. 点击分类可筛选商家 3. 选中态样式正确 |

### UC-705 首页 - 商家卡片展示

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-705 |
| 测试目标 | 验证首页商家网格显示 |
| 操作 | 进入首页 |
| 验证点 | 1. 商家卡片显示图片 2. 显示名称、评分、月售、配送时间 3. 促销标签显示 4. 响应式布局正常 |

### UC-706 首页 - 搜索功能

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-706 |
| 测试目标 | 验证搜索栏过滤商家 |
| 操作 | 在搜索框输入商家关键词 |
| 验证点 | 商家列表实时过滤 |

### UC-707 首页 - 附近拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-707 |
| 测试目标 | 验证附近拼单列表显示 |
| 操作 | 进入首页，查看"附近拼单"区域 |
| 验证点 | 1. 显示可加入的拼单 2. 每个有"+"按钮 3. 无拼单时显示"暂无可加入的拼单" |

### UC-708 商家详情页

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-708 |
| 测试目标 | 验证点击商家进入详情页 |
| 操作 | 点击商家卡片 |
| 验证点 | 1. 显示商家大图和信息 2. 菜品按分类分组显示 3. 菜品有图片、名称、价格、"+"按钮 |

### UC-709 购物车 - 添加菜品

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-709 |
| 测试目标 | 验证添加菜品到购物车 |
| 操作 | 在商家详情页点击"+"按钮 |
| 验证点 | 1. 底部购物车栏出现 2. 数量和总价更新 3. 弹出 toast "已添加 xxx" 4. 按钮有弹跳动画 |

### UC-710 购物车 - 多件添加

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-710 |
| 测试目标 | 验证同一菜品添加多次数量累加 |
| 操作 | 点击同一菜品"+"按钮 3 次 |
| 验证点 | 购物车该菜品数量为 3 |

### UC-711 购物车 - 切换商家清空

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-711 |
| 测试目标 | 验证切换商家时提示清空购物车 |
| 操作 | 在商家 A 添加菜品 → 返回 → 进入商家 B 添加 → 确认 |
| 验证点 | 弹出确认对话框，确认后购物车清空并重新添加 |

### UC-712 从购物车发起拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-712 |
| 测试目标 | 验证从购物车创建拼单 |
| 操作 | 添加菜品 → 点击"去开团" → 填写信息 → 点击"发起拼单" |
| 验证点 | 1. 商家名自动填入 2. 创建成功后菜品自动添加 3. 跳转到拼单详情 |

### UC-713 加入附近拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-713 |
| 测试目标 | 验证从首页加入附近拼单 |
| 操作 | 首页"附近拼单" → 点击"+" → 选择地址 → 选择菜品 → 加入 |
| 验证点 | 加入成功，toast 提示 |

### UC-714 拼单 Tab - 我的拼单列表

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-714 |
| 测试目标 | 验证拼单 Tab 显示用户参与的拼单 |
| 操作 | 点击"拼单" Tab |
| 验证点 | 1. 显示拼单列表 2. 每项有商家名、状态标签、时间 3. 空状态有"去发现"链接 |

### UC-715 拼单详情页

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-715 |
| 测试目标 | 验证拼单详情页显示 |
| 操作 | 点击拼单列表项 |
| 验证点 | 1. 显示拼单信息 2. 参与者列表 3. 每人菜品和金额 4. 邀请码 5. 有"退出"按钮 |

### UC-716 退出拼单

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-716 |
| 测试目标 | 验证从拼单详情退出 |
| 操作 | 拼单详情 → 点击"退出" → 确认 |
| 验证点 | 1. 弹出确认对话框 2. 确认后退出成功 3. 返回上一页 |

### UC-717 订单 Tab

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-717 |
| 测试目标 | 验证订单 Tab 功能 |
| 操作 | 点击"订单" Tab |
| 验证点 | 1. 显示订单列表 2. PENDING_CONFIRM 状态有取消按钮 3. 空状态正确 |

### UC-718 支付 Tab

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-718 |
| 测试目标 | 验证支付记录 Tab |
| 操作 | 点击"支付" Tab |
| 验证点 | 1. 显示扣款/退款记录 2. 类型标签正确（CHARGE/REFUND） 3. 金额格式正确 |

### UC-719 登出功能

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-719 |
| 测试目标 | 验证用户登出 |
| 操作 | 点击"退出"按钮 |
| 验证点 | 1. 清除 localStorage 2. 跳转到登录页 3. 购物车清空 |

### UC-720 页面导航返回

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-720 |
| 测试目标 | 验证页面返回按钮正常工作 |
| 操作 | 首页 → 商家详情 → 返回 → 应该回到首页 |
| 验证点 | 返回栈正确，不会丢失状态 |

---

## 8. 集成测试用例（端到端）

### UC-801 完整拼单流程

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-801 |
| 测试目标 | 验证从注册到成团的完整流程 |
| 步骤 | 1. 用户 A 注册 2. 用户 B 注册 3. 添加地址 4. A 创建拼单（minMembers=2）5. B 加入拼单 6. 验证自动成团 7. 查询订单 8. 查询结算 |
| 验证点 | 全链路无报错，状态流转正确 |

### UC-802 拼单超时解散流程

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-802 |
| 测试目标 | 验证拼单超时自动解散 |
| 步骤 | 1. 创建拼单（deadlineMinutes=1） 2. 等待 1+ 分钟 3. 尝试加入 |
| 验证点 | 超时后拼单被解散，触发退款 |

### UC-803 取消订单触发退款流程

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-803 |
| 测试目标 | 验证取消订单后自动退款 |
| 步骤 | 1. 拼单成团 2. 订单创建 3. 扣款 4. 取消订单 5. 查询退款记录 |
| 验证点 | 退款金额 = 扣款金额 |

### UC-804 商家浏览到拼单全流程

| 项目 | 内容 |
|------|------|
| 用例编号 | UC-804 |
| 测试目标 | 验证前端从商家浏览到创建拼单的全流程 |
| 步骤 | 1. 登录 2. 首页浏览商家 3. 点击商家查看菜单 4. 添加菜品到购物车 5. 去开团 6. 填写信息创建 7. 查看拼单详情 |
| 验证点 | 前端流程顺畅，数据正确传递 |
